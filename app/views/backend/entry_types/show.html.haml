%h2.section-title.st-blue Cennik
.row
  .col-md-6
    .card-panel
      .blue-text.text-darken-2
        %i.fa.fa-info-circle
        Jesteś na stronie zakupu wejściówki na teren aquaparku.
        Poniżej wybierz interesujący Cię rodzaj biletu/karnetu a następnie kliknij "KUP TERAZ".
.divider-new
  Bilety
.row
  #carousel-multi-item.carousel.slide.multiitem-car
    .text-center
      %a.btn-floating.btn-small.waves-effect.waves-light.grey.darken-4{"data-slide" => "prev", :href => "#carousel-multi-item", :role => "button"}
        %i.material-icons keyboard_arrow_left
      %a.btn-floating.btn-small.waves-effect.waves-light.grey.darken-4{"data-slide" => "next", :href => "#carousel-multi-item", :role => "button"}
        %i.material-icons keyboard_arrow_right
    / Indicators
    %ol.carousel-indicators
      %li.active{"data-slide-to" => "0", "data-target" => "#carousel-multi-item"}
      %li{"data-slide-to" => "1", "data-target" => "#carousel-multi-item"}
      %li{"data-slide-to" => "2", "data-target" => "#carousel-multi-item"}
    / Wrapper for slides
    .carousel-inner
      - groups = @tickets.in_groups_of(4, false)
      - groups.each_with_index do |group, i|
        - if i == 0
          .item.active
            .row.text-center
              - group.each do |field|
                .col-md-3
                  .card.hoverable.success-color-dark
                    .card-content.white-text
                      %h5
                        = field.kind
                      %h4.blue-text
                        = field.price
                        ZŁ
                      %p
                        = field.kind_details
                      %p
                        = field.description
                    / Buttons
                    .card-action.text-center
                      = link_to new_backend_entry_type_bought_detail_path(field) do
                        .fa.fa-shopping-cart
                        Kup Teraz
        - else
          .item
            .row.text-center
              - group.each do |field|
                .col-md-3
                  .card.hoverable.success-color-dark
                    .card-content.white-text
                      %h5
                        = field.kind
                      %h4.blue-text
                        = field.price
                        ZŁ
                      %p
                        = field.kind_details
                      %p
                        = field.description
                    / Buttons
                    .card-action.text-center
                      = link_to new_backend_entry_type_bought_detail_path(field) do
                        .fa.fa-shopping-cart
                        Kup Teraz
.divider-new
  Karnety
.row
  #carousel-multi-item.carousel.slide.multiitem-car
    .text-center
      %a.btn-floating.btn-small.waves-effect.waves-light.grey.darken-4{"data-slide" => "prev", :href => "#carousel-multi-item", :role => "button"}
        %i.material-icons keyboard_arrow_left
      %a.btn-floating.btn-small.waves-effect.waves-light.grey.darken-4{"data-slide" => "next", :href => "#carousel-multi-item", :role => "button"}
        %i.material-icons keyboard_arrow_right
    / Indicators
    %ol.carousel-indicators
      %li.active{"data-slide-to" => "0", "data-target" => "#carousel-multi-item"}
      %li{"data-slide-to" => "1", "data-target" => "#carousel-multi-item"}
      %li{"data-slide-to" => "2", "data-target" => "#carousel-multi-item"}
    / Wrapper for slides
    .carousel-inner
      - groups = @passes.in_groups_of(4, false)
      - groups.each_with_index do |group, i|
        - if i == 0
          .item.active
            .row.text-center
              - group.each do |field|
                .col-md-3
                  .card.hoverable.primary-color-dark
                    .card-content.white-text
                      %h5
                        = field.kind
                      %h4.blue-text
                        = field.price
                        ZŁ
                      %p
                        = field.kind_details
                      %p
                        = field.description
                    / Buttons
                    .card-action.text-center
                      = link_to new_backend_entry_type_bought_detail_path(field) do
                        .fa.fa-shopping-cart
                        Kup Teraz
        - else
          .item
            .row.text-center
              - group.each do |field|
                .col-md-3
                  .card.hoverable.primary-color-dark
                    .card-content
                      %h5
                        = field.kind
                      %h4.blue-text
                        = field.price
                        ZŁ
                      %p
                        = field.kind_details
                      %p
                        = field.description
                    / Buttons
                    .card-action.text-center
                      = link_to new_backend_entry_type_bought_detail_path(field) do
                        .fa.fa-shopping-cart
                        Kup Teraz
    / /.Buttons
  / Image Card
